{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp", "--browser", "msedge"],
      "enabled": false,
      "description": "Browser automation via Playwright (disabled — shelved project). Can navigate, click, type, screenshot, and extract data from web pages."
    }
  }
}
